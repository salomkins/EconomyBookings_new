{
  "name": "EconomyBookings",
  "version": "0.0.1",
  "description": "School project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass --include-path src/scss src/scss/index.scss dist/css/style.css",
    "watch": "nodemon -e scss -x 'npm run build-css'"
  },
  "author": "The Fresh Leopards TM",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.5.3",
    "nodemon": "^1.11.0"
  }
}
